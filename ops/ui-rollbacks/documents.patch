diff --git a/src/components/views/DocumentsView.tsx b/src/components/views/DocumentsView.tsx
index 48cfe75..7e165a2 100644
--- a/src/components/views/DocumentsView.tsx
+++ b/src/components/views/DocumentsView.tsx
@@ -1,8 +1,6 @@
 "use client";
 import {useCallback,useEffect,useRef,useState} from 'react';
 import dynamic from 'next/dynamic';
-import Link from 'next/link';
-import {ChevronRightIcon,CheckSquareIcon,MessageIcon,TargetIcon} from '@/components/Icons';
 import {documentRequest,fileBase64,validateDocumentFiles,type LearnerDocument,type DocumentMessage} from '@/lib/documents';
 import {isStudentPreview} from '@/lib/student-preview';
 import FeedbackText from '@/components/FeedbackText';
@@ -56,8 +54,7 @@ export default function DocumentsView({code,name,teacher=false,parent=false}:{co
   }
   function revision(){if(!selected)return;setParentId(selected.id);setTitle(`Revision: ${selected.title}`.slice(0,150));setContext(selected.context);setFiles([]);uploadId.current=crypto.randomUUID();uploadForm.current?.scrollIntoView({behavior:'smooth',block:'start'});setNotice('Add the revised work above. The earlier version will stay saved.');}
   return <div className="documents-space">
-    <section className="doc-hero"><div><p className="doc-eyebrow">{teacher?`${name.toUpperCase()}’S LEARNING FILE`:'YOUR SAVED WORK'}</p><h1>{teacher?'Documents & feedback':'Documents'}</h1><p>{teacher?`Scan or upload work for ${name}. Originals, practice feedback and follow-up questions stay together here.`:'Scan or upload a page, then find the original and Rory’s feedback here.'}</p></div><div className="doc-paper-art" aria-hidden="true"><span className="doc-art-spark">✦</span><div><i/><i/><i/><b>one small<br/>step forward.</b><i/></div><span className="doc-art-badge">✓</span></div></section>
-    {!teacher&&!parent&&<nav className="ux-route-grid" aria-label="Related work pages"><Link className="ux-route-card" href={`/s/${code}/homework/`}><CheckSquareIcon/><span><strong>Homework</strong><small>Return to the task</small></span><ChevronRightIcon/></Link><Link className="ux-route-card" href={`/s/${code}/progress/`}><MessageIcon/><span><strong>Feedback</strong><small>Rory’s next steps</small></span><ChevronRightIcon/></Link><Link className="ux-route-card" href={`/s/${code}/test-prep/`}><TargetIcon/><span><strong>Test prep</strong><small>Practice for a test</small></span><ChevronRightIcon/></Link></nav>}
+    <section className="doc-hero"><div><p className="doc-eyebrow">{teacher?`${name.toUpperCase()}’S LEARNING FILE`:'FROM PAGE TO PROGRESS'}</p><h1>{teacher?'Documents & feedback':'My documents'}</h1><p>{teacher?`Scan or upload work for ${name}. Originals, practice feedback and follow-up questions stay together here.`:'A photo of your page. A little help with the tricky parts. A chance to try again.'}</p></div><div className="doc-paper-art" aria-hidden="true"><span className="doc-art-spark">✦</span><div><i/><i/><i/><b>one small<br/>step forward.</b><i/></div><span className="doc-art-badge">✓</span></div></section>
     <ol className="doc-steps" aria-label="How it works"><li><span>1</span><div>Save your work<small>Scan pages or upload a file</small></div></li><li><span>2</span><div>Read & explore<small>AI suggestions and questions</small></div></li><li><span>3</span><div>Feedback & retry<small>Rory’s review, then your next draft</small></div></li></ol>
     {preview&&<p className="doc-notice">Student preview is read-only. Add documents from the teacher workspace.</p>}
     {!parent&&<form ref={uploadForm} className="doc-upload-card" onSubmit={e=>void upload(e)}>
