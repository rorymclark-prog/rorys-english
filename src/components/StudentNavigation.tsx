"use client";
import Link from "next/link";
import {usePathname} from "next/navigation";
import {useStudent} from "./StudentContext";
import {HomeIcon,BookIcon,CheckSquareIcon,ChartIcon,GearIcon} from "./Icons";
import {MicrophoneIcon,RepeatIcon} from "./LearningVisuals";
export default function StudentNavigation(){const {code,displayName}=useStudent(),path=usePathname();const base=`/s/${code}`;const links=[["","Today",HomeIcon],["homework/","Homework",CheckSquareIcon],["documents/","My documents",BookIcon],["speak/","Speaking studio",MicrophoneIcon],["study/","Practice",RepeatIcon],["resources/","Lesson library",BookIcon],["progress/","My progress",ChartIcon]] as const;return <aside className="re-sidebar"><Link href={`${base}/`} className="re-brand"><span>r.</span><div>Rory’s English<small>A LITTLE EVERY LESSON.</small></div></Link><div className="re-workspace-label"><BookIcon width={19}/><div>YOUR LEARNING SPACE<strong>{displayName}</strong></div></div><nav aria-label="Workspace navigation">{links.map(([suffix,label,Icon])=>{const href=`${base}/${suffix}`,active=suffix?path.startsWith(href):path===base||path===base+"/";return <Link key={href} href={href} aria-current={active?"page":undefined} className={active?"is-active":""}><Icon width={21} height={21}/>{label}</Link>})}</nav><div className="re-sidebar-bottom"><div className="re-encouragement"><span aria-hidden="true">✦</span><strong>Small steps.<br/>Real progress.</strong><p>Space to practise, make mistakes and try again.</p></div><Link href={`${base}/settings/`} className="re-settings-link"><GearIcon width={20}/>Settings & appearance</Link><div className="re-profile"><span>{displayName[0]}</span><div><strong>{displayName}</strong><small>Your English space</small></div></div></div></aside>}
