#!/usr/bin/env bash
# Historical Pages publication is retired; retained to prevent accidental reuse.
set -euo pipefail
echo "This app now needs its Vercel server API. Publish to the existing project with npm run deploy."
exit 1
