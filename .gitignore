/node_modules
/.next/
/.next-dev/
/out/
/build

# private local environment and credentials
.env*
!.env.local.example
!.env.example
.DS_Store
*.pem
npm-debug.log*
firebase-debug.log*
.vercel/
*.tsbuildinfo
next-env.d.ts

# Generated by `npm run prebuild` (scripts/gen-pwa.mjs) before every build.
# Committing them meant a diff on every build and a stale placeholder cache
# name sitting in the repo. The sources are scripts/service-worker.js and
# content/students.json.
/public/sw.js
/public/m/*.webmanifest
